language: cpp
sudo: required
script: cmake . && make
os:
- windows
- linux
branches:
  only:
  - master
addons:
  artifacts: true
deploy:
  provider: releases
  api_key:
  file: "test.tar.gz"
  skip_cleanup: true
  on:
    tags: true

